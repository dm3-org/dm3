import axios from 'axios';
describe('storage', () => {
    it('should pass', () => {
        expect(true).toBe(true);
    });
    it('should pass', () => {
        expect(true).toBe(false);
    });
});
